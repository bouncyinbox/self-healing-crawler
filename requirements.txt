# This file exists for backward compatibility with tools that don't read pyproject.toml.
# The authoritative dependency list is in pyproject.toml.
# Install with: pip install -e ".[dev]"

playwright>=1.44.0
anthropic>=0.28.0
pydantic>=2.0.0
pydantic-settings>=2.4.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
imagehash>=4.3.1
Pillow>=10.0.0
aiosqlite>=0.20.0
aiohttp>=3.9.0
aiofiles>=23.0.0
rich>=13.0.0
click>=8.1.0
python-dotenv>=1.0.0

# Dev
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
coverage>=7.0.0
